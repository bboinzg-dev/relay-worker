diff --git a/src/utils/db.js b/src/utils/db.js
index c787a9146e3b8c6817525507bd4bdc235ef58819..27e32598310e6a37bcee717207debbc8e7fb86c1 100644
--- a/src/utils/db.js
+++ b/src/utils/db.js
@@ -1,16 +1,26 @@
 'use strict';
 const { getPool } = require('../../lib/db');
 
 const pool = getPool();
 
+const origQuery = pool.query.bind(pool);
+pool.query = async (text, params) => {
+  if (process.env.VERBOSE_TRACE === '1') {
+    const caller = new Error().stack.split('\n')[2]?.trim();
+    const head = String(text).split('\n')[0].slice(0, 140);
+    console.log(`[SQL] ${head} :: caller=${caller}`);
+  }
+  return origQuery(text, params);
+};
+
 async function query(text, params) {
   return pool.query(text, params);
 }
 
 module.exports = {
   pool,
   query,
   getPool,
 };
 
 module.exports.db = pool;
