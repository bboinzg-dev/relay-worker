{
  "name": "relay-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloud Run worker (Express + pg)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=20"
  }
}
